/* Add these at the bottom of globals.css */

/* Pill mode selector */
.pill-selector {
  position: relative;
  display: flex;
  background: rgba(255,255,255,0.04);
  border-radius: 12px;
  padding: 4px;
  border: 1px solid rgba(124,58,237,0.2);
}
.pill-slider {
  position: absolute;
  top: 4px; bottom: 4px;
  background: linear-gradient(135deg, var(--purple), var(--blue));
  border-radius: 9px;
  transition: left 0.3s cubic-bezier(0.23,1,0.32,1), width 0.3s;
  box-shadow: 0 0 16px rgba(124,58,237,0.4);
}
.pill-option {
  flex: 1;
  position: relative;
  z-index: 1;
  background: none;
  border: none;
  color: var(--text-3);
  font-size: 13px;
  font-weight: 600;
  padding: 8px 4px;
  cursor: pointer;
  font-family: 'Syne', sans-serif;
  transition: color 0.2s;
  border-radius: 9px;
  letter-spacing: 0.02em;
}
.pill-option.active { color: #fff; }

/* Drop zone */
.drop-zone {
  border: 2px dashed rgba(255,255,255,0.1);
  transition: border-color 0.2s, background 0.2s;
}
.drop-zone.dragging {
  border-color: var(--purple);
  background: rgba(124,58,237,0.06);
}
.drop-zone:hover { border-color: rgba(124,58,237,0.4); }

/* Shimmer text */
.shimmer-text {
  background: linear-gradient(90deg, #a78bfa 0%, #60a5fa 40%, #a78bfa 80%);
  background-size: 200% auto;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  animation: shimmer 3s linear infinite;
}
